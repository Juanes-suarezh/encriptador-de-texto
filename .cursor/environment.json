{
  "name": "Encriptador de texto",
  "install": "echo 'Static site: no dependencies to install.'",
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Static web server serving the Encriptador de texto app at http://localhost:8000"
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 8000
    }
  ]
}
